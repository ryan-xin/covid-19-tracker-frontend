import React, { useState, useEffect } from 'react';
import axios from 'axios';

const Cases = (props) => {
  return(
    <div>
      <h1>All New Cases</h1>
    </div>
  );
}; // Cases

export default Cases;